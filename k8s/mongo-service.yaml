apiVersion: v1
kind: Service
metadata:
  name: mongo
  labels:
    app: policy-claims-db
    component: database
spec:
  selector:
    app: policy-claims-db
    component: database
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
  type: ClusterIP
