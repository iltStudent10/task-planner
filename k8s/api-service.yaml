apiVersion: v1
kind: Service
metadata:
  name: task-manager-api
  labels:
    app: task-manager-api
    component: api
spec:
  type: NodePort
  selector:
    app: task-manager-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
